# credit-risk-classification

# Objective

Import and convert the lending_data.csv file. 

#Split the data into training and testing datasets setting y from the loan-status column and x from the reamining columns. Create a Logistic Regression Model and fit the date. Evaluate the model's performance by generating a confusion matrix and classification report. 
